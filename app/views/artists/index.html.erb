<h1>Artists</h1>

<ul>
  <% @artists.each do |a| %>
    <li><%= link_to a.name, artist_path(a) %></li>
       <%= link_to a.name, artist_path(a) %> (<%= pluralize(artist.songs.count, "song") %>)
  <% end %>
</ul>
